{% extends "base.html.twig" %}

{% block body %}
    <h1>Voulez vous vraiment supprimer la personne {{ personne.nom }} ?</h1>
    {{ form(formulaire) }}

    <a class="btn btn-warning" href="{{ path("personnes") }}">&lt; retour à la liste</a>

{% endblock %}